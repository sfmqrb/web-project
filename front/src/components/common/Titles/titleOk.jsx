const TitleOk = (props) => {
  return (
    <p
      style={{
        color: "#555",
        fontSize: "20px",
        margin: "0px",
        verticalAlign: "middle",
        paddingTop: "5px",
      }}>
      {props.title}
    </p>
  );
};

export default TitleOk;
