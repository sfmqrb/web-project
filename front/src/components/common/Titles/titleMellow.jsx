const TitleMellow = (props) => {
  return (
    <p
      style={{
        color: "#aaa",
        fontSize: "16px",
        margin: "0px",
        marginTop: "20px",
      }}>
      {props.title}
    </p>
  );
};

export default TitleMellow;
