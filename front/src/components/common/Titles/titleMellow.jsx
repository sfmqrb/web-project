const TitleMellow = (props) => {
  return (
    <p
      style={{
        color: "#aaa",
        fontSize: "16px",
        margin: "0px",
        verticalAlign: "middle",
        marginTop: "5px",
      }}>
      {props.title}
    </p>
  );
};

export default TitleMellow;
